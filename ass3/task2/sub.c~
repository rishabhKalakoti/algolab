#include<stdio.h>
int max(int a,int b)
{
	if(a>b)
		return a;
	else
		return b;
}
int st[500];
int* stPtr;
int main()
{
	int arr[500];
	int i=0,j;
	int x;
	while(scanf("%d",&x) > 0)
	{
		arr[i]=x;
		i++;
	}
	/*
	int l=i;
	int maxN=0;
	int count=0;
	int c[l];
	for(i=l-1; i>=0; i--)
	{
		count=0;
		for(j=i+1;j<l;j++)
		{
			if(arr[i]<arr[j])
				count=count+c[j]+1;
		}
		c[i]=count;
		maxN=max(maxN,count);
	}
	*/
	// printf("Number of sequences is %d\n",maxN);
	
	return 1;
}
